# env.example — copy this to .env and fill in your own values
# NEVER commit .env to Git — it contains real secrets!
#
# cp env.example .env
# then edit .env with your passwords

DB_NAME=studentdb
DB_USER=smsuser
DB_PASSWORD=change_me_strong_password
DB_ROOT_PASSWORD=change_me_root_password
SECRET_KEY=change_me_flask_secret_key
